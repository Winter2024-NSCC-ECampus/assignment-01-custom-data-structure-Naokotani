#ifndef __HEADER_H__
#define __HEADER_H__
#include "linkedList.h"
#include "parse.h"

#endif

LinkedList extractMobyDick();
LinkedList randomQuote(LinkedList list);
int random_number(int min_num, int max_num);
